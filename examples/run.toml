schema_version = 1
cluster = "heterogeneous_cluster.toml"
workload = "trace_workload.toml"

[output]
format = "json"
top_k = 1
request_limit = 1

[search]
max_parallelism_candidates = 8
max_prefill_candidates = 4
max_decode_candidates = 4
max_serving_pairs = 12
